import { LitElement, TemplateResult, customElement, html } from "lit-element";

@customElement("o-dam")
export default class ODam extends LitElement {
  render(): TemplateResult {
    return html`
      <h1>o-dam</h1>
    `;
  }
}
